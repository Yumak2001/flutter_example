enum ScreenStatus {
  loading,
  view,
  error,
}

enum HomeScreens {
  main,
  laboratory,
  ar,
  mapLocation,
  profile,
}

enum AuthPageStatus {
  login,
  registration,
  resetPass,
  editPass,
}
